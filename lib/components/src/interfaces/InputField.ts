// Enum for Input Field Type
export enum InputFieldType {
  TEXT = "text",
  NUMBER = "number",
  EMAIL = "email",
  PASSWORD = "password",
  DATE = "date",
  TELEPHONE = "tel",
}

// Enum for Input Field Size
export enum InputFieldSize {
  SMALL = "small",
  LARGE = "large",
}
